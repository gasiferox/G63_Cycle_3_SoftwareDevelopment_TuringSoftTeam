@page
@model HospiEnCasa.App.Frontend.Pages.Pacientes.Signos.ListModel
@{
    @* int idPaciente = Model.Paciente.Id; *@
}

<h1>Signos Vitales del Paciente</h1>

<table class="table">
    @* @foreach (var signo in Model.SignosPaciente)
    {
        <tr>
            <td>@signo.TipoSigno</td>
            <td>@signo.Valor</td>
        </tr>
    } *@

</table>